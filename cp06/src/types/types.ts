export type TipoAluno = {
    rm: string;
    nome: string;
    area: string;
    desc: string;
    // projetos: {nome:string, desc:string, nota:number}[];
    github: string;
    linkedin: string;
    email: string;
}